плагин для postHTML, превращающий все недопустимые aria-like атрибуты в настоящие aria-атрибуты:

    <button pressed> => <button aria-pressed>

----

плагин для postCSS, делающий то же самое

----

плагин для postCSS, добавляющий скоуп всем селекторам указанной таблицы стилей

1. cascade-based scope:

    button { color: #aaa } => .yandex-theme button { color: #aaa }

2. mixin-based scope:

    button { color: #aaa } => button.yandex-theme { color: #aaa }

----

применять стили к кнопке в виде иконки только при наличии непустого атрибута title

button[title].icon
{
    line-height: 0;
    padding: 5px 3px;
}

